Dev, PelleNybe/Corax CoLAB: [performance improvement]

💡 What: Implemented multiple performance and UI improvements across the application.
- Added hashlib.sha256 hashing for cache file names in data_fetcher.py to prevent path traversal and ensure proper sanitization.
- Optimized equity curve generation in backtester.py by directly appending dictionaries, avoiding an O(n) tuple-to-dict conversion loop.
- Added st.spinner loading state during analysis in Streamlit app.py.
- Improved visual polish in Plotly Equity Curve with a custom hovertemplate.
- Enforced yf.set_tz_cache_location early in main.py to prevent concurrent thread collision issues.
- Fixed os.makedirs bug with exist_ok=True in data_fetcher.py.

🎯 Why: Addressed performance overhead of building tuples and then converting to dictionaries for large backtest datasets. Enhanced security and application resilience to crashes due to yfinance caching. Improved User Experience and feedback during AI evaluation.

📊 Impact: Reduces time complexity of final equity curve rendering by avoiding a complete array traversal, resulting in faster backtest completions. UI feels much more responsive with loading indicators and better graph tooltips. Secure hashing eliminates directory traversal vectors completely.

🔬 Measurement: Run pytest suite, inspect terminal and app UI. Streamlit app will now present a loading spinner immediately when hitting 'Run Analysis', and historical runs execute slightly faster without tuple overhead.
